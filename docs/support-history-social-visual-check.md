# Support history and social promotion visual check

The desktop and mobile full-page captures show the authenticated account page remains readable with the new Support History section placed below the workspace content. The empty state is clear when no persisted support tickets exist. The homepage WhatsApp promotion now supports two actions: Join WhatsApp Channel and Join Instagram; the mobile layout wraps the actions rather than forcing horizontal overflow. The support toast is triggered from the real mutation success path, while ticket history reads only from the authenticated `support.mine` procedure and displays persisted admin notes when present. The current preview session was authenticated as a Job Seeker, so no test ticket was created or modified during visual verification.
